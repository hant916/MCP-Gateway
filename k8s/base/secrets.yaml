# NOTE: These are templates. DO NOT commit actual secrets to git!
# Use sealed-secrets, external-secrets, or kubectl create secret in production

apiVersion: v1
kind: Secret
metadata:
  name: mcp-gateway-db
  labels:
    app: mcp-gateway
type: Opaque
stringData:
  jdbc-url: "jdbc:postgresql://postgres.default.svc.cluster.local:5432/mcpgateway"
  username: "REPLACE_WITH_ACTUAL_USERNAME"
  password: "REPLACE_WITH_ACTUAL_PASSWORD"

---
apiVersion: v1
kind: Secret
metadata:
  name: mcp-gateway-redis
  labels:
    app: mcp-gateway
type: Opaque
stringData:
  password: "REPLACE_WITH_ACTUAL_REDIS_PASSWORD"

---
apiVersion: v1
kind: Secret
metadata:
  name: mcp-gateway-jwt
  labels:
    app: mcp-gateway
type: Opaque
stringData:
  secret-key: "REPLACE_WITH_ACTUAL_JWT_SECRET_256_BITS"

---
apiVersion: v1
kind: Secret
metadata:
  name: mcp-gateway-stripe
  labels:
    app: mcp-gateway
type: Opaque
stringData:
  secret-key: "REPLACE_WITH_ACTUAL_STRIPE_SECRET_KEY"
  webhook-secret: "REPLACE_WITH_ACTUAL_STRIPE_WEBHOOK_SECRET"
